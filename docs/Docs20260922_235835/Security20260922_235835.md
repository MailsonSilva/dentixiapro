# Relatório de Segurança Consolidado — Deduplicado

## Sumário Executivo

### Total de Findings por Severidade

| Severidade | Quantidade |
|------------|-----------|
| CRITICO    | 4         |
| ALTO       | 4         |
| MEDIO      | 14        |
| BAIXO      | 15        |
| **Total**  | **37**    |

### Top 3 Áreas Mais Críticas

1. **Isolamento entre tenants e controle de acesso** — políticas RLS permissivas em `public.usuarios`, exposição do `company_id` via IDOR e elevação de privilégio via cadastro (`tipo=parceiro`) e auto-update da própria linha para `admin`.
2. **Fluxo de faturamento/checkout sem autenticação ou validação robusta** — edge function `create-checkout` com autenticação opcional, server action que envia payload arbitrário, CORS permissivo e open redirect no `create-portal`.
3. **Exposição de dados sensíveis de saúde (LGPD) no Storage** — bucket `simulacoes` criado como público e nunca alterado para privado, servindo fotos odontológicas de pacientes por URL sem autenticação.

### Esforço Total Estimado

| Severidade | Findings | Esforço estimado (h) |
|------------|----------|----------------------|
| CRITICO    | 4        | 32h                  |
| ALTO       | 4        | 24h                  |
| MEDIO      | 14       | 56h                  |
| BAIXO      | 15       | 30h                  |
| **Total**  | **37**   | **142h (~18 dias-dev)** |

---

## Nota de Deduplicação

As seguintes entradas foram removidas por apontarem para o mesmo problema no mesmo arquivo, mantendo-se a versão mais completa:

| Removido(s) | Mantido como |
|-------------|--------------|
| AUTH-001 | CRITICO-001 (origem ISOLATION-002) |
| AUTH-003, OWASP-001 | CRITICO-003 (origem ISOLATION-001) |
| AUTH-004, LOGIC-002 | MEDIO-001 (origem OWASP-002) |
| OWASP-010 | MEDIO-002 (origem AUTH-005) |
| OWASP-004 | BAIXO-001 (origem AUTH-006) |
| OWASP-009 | BAIXO-003 (origem AUTH-008) |
| OWASP-006 | BAIXO-004 (origem AUTH-009) |
| OWASP-007 | MEDIO-005 (origem LOGIC-005) |

---

## Findings Críticos

---

### CRITICO-001: Auto-atualização da tabela `usuarios` permite escalar `tipo` para admin e acessar dados de outros tenants
- **Severidade:** CRITICO
- **Arquivo(s):** supabase/migrations/20260701000000_core_users_auth_and_security.sql:41 (política recriada em supabase/migrations/20260707000000_security_fixes_rls_and_storage.sql:30)
- **Trecho:**
  ```sql
  CREATE POLICY "Admins can update all users" 
  ON public.usuarios FOR UPDATE 
  TO authenticated 
  USING (
    auth.uid() = id OR public.is_admin(auth.uid())
  );
  ```
- **Impacto:** A política permite que qualquer usuário autenticado atualize a própria linha (`auth.uid() = id`), sem restrição de colunas e sem `WITH CHECK`. Como o Supabase concede UPDATE sobre as colunas das tabelas do schema `public` ao role `authenticated`, o usuário pode executar `UPDATE usuarios SET tipo = 'admin' WHERE id = auth.uid()`. A função `is_admin()` (SECURITY DEFINER) passa então a retornar `true`, liberando o acesso a dados de todos os usuários/tenants por meio das policies de SELECT de `usuarios`, `simulacoes`, `simulacao_tracking`, `user_company`, `customers` e `subscriptions`. Também permite alterar `trial_ends_at` (burlar paywall) e `is_blocked` (auto-desbloqueio).
- **Recomendacao:** Separar a política de self-update da política de admin e impedir alteração de colunas sensíveis. Ex.: revogar UPDATE nas colunas críticas (`REVOKE UPDATE (tipo, is_blocked, trial_ends_at, commission_rate, referral_code, referred_by_code) ON public.usuarios FROM authenticated;`), adicionar `WITH CHECK` explícito e/ou um trigger que impeça não-admins de alterar `tipo`. A política de admin deve usar apenas `USING (public.is_admin(auth.uid()))` para atualização de terceiros, e a auto-atualização deve ser limitada a campos de perfil (ex.: `nome_completo`, `telefone`, `logo_url`).

---

### CRITICO-002: Autenticação opcional no endpoint `create-checkout` permite criação de sessão Stripe e escrita no banco sem login
- **Severidade:** CRITICO
- **Arquivo(s):** supabase/functions/create-checkout/index.ts:33-42; supabase/functions/create-checkout/index.ts:101-150
- **Trecho:**
  ```ts
  // Validação opcional de JWT se fornecido no cabeçalho
  const authHeader = req.headers.get("Authorization");
  let authenticatedUser: any = null;
  if (authHeader && authHeader.startsWith("Bearer ")) {
    const token = authHeader.replace("Bearer ", "").trim();
    if (token && token !== Deno.env.get("SUPABASE_ANON_KEY")) {
      const { data: { user } } = await supabase.auth.getUser(token);
      authenticatedUser = user;
    }
  }
  ```
- **Impacto:** A autenticação é tratada como opcional e é explicitamente ignorada quando o token enviado é a anon key — exatamente o que o frontend faz em `src/lib/planos/actions.ts` (`Authorization: Bearer ${anonKey}`). Sem usuário autenticado, todo o bloco de verificação de IDOR (`company_id` vs. usuário) é pulado, e a função segue usando a **service role key** para fazer `upsert` em `company` e `customers` e criar sessões de checkout do Stripe para qualquer `company_id`/`price_id` fornecidos. Um atacante não autenticado (e sem CORS restrito) pode manipular dados de faturamento, criar sessões de pagamento fraudulentas e potencialmente sequestrar o vínculo `customers.stripe_customer_id` de uma empresa.
- **Recomendacao:** Tornar a autenticação obrigatória: exigir um JWT de usuário válido (nunca aceitar anon key como credencial de identidade), retornar 401 se ausente/inválido e sempre executar a verificação de propriedade/`user_company`/admin antes de qualquer escrita. Além disso, nunca usar a service role key em endpoints acessíveis publicamente sem autenticação estrita.

---

### CRITICO-003: Bucket `simulacoes` permanece público e expõe fotos de pacientes via URL
- **Severidade:** CRITICO
- **Arquivo(s):** supabase/migrations/20260702000000_storage_buckets_and_rls.sql:8 e supabase/migrations/20260707000000_security_fixes_rls_and_storage.sql:43
- **Trecho:**
  ```sql
  INSERT INTO storage.buckets (id, name, public)
  VALUES 
    ('dentixia', 'dentixia', true),
    ('simulacoes', 'simulacoes', true),
    ('logoEmpresa', 'logoEmpresa', true)
  ON CONFLICT (id) DO NOTHING;
  ```
  ```sql
  CREATE POLICY "Leitura restrita para simulacoes" ON storage.objects
  FOR SELECT
  TO authenticated
  USING (
    bucket_id = 'simulacoes' 
    AND (
      (storage.foldername(name))[1] = auth.uid()::text 
      OR public.is_admin(auth.uid())
    )
  );
  ```
- **Impacto:** O bucket `simulacoes` armazena fotos médicas de pacientes (imagem original e imagem simulada por IA). Ele foi criado com `public = true` e **nunca foi alterado para privado**. A migration de "security fixes" apenas adicionou uma política RLS de SELECT, mas em buckets públicos do Supabase Storage os objetos são servidos diretamente pela URL pública/CDN, sem passar pela RLS. O próprio código da aplicação usa `getPublicUrl()` (`src/lib/simulacoes/actions.ts` e `src/lib/actions/simulacoes.ts`), gerando URLs acessíveis por qualquer pessoa que as obtenha. Isso anula o isolamento entre tenants/usuários e vaza dados sensíveis de saúde (fotos de pacientes) para qualquer um com o link.
- **Recomendacao:** Tornar o bucket privado: `UPDATE storage.buckets SET public = false WHERE id = 'simulacoes';` e passar a usar URLs assinadas (`createSignedUrl`) ou download autenticado via client Supabase. Manter a política RLS como defesa em profundidade para o acesso via Storage API. Revisar também o conteúdo do bucket `logoEmpresa` caso não deva ser intencionalmente público.

---

### CRITICO-004: Escalação de privilégio — qualquer usuário pode se auto-declarar "parceiro" no cadastro e burlar o paywall
- **Severidade:** CRITICO
- **Arquivo(s):** `src/lib/auth/actions.ts` (signUpAction), `supabase/migrations/20260706000000_cleanup_and_referral_fix.sql` (handle_new_user), `src/lib/auth/actions.ts` (getClientLayoutDataAction)
- **Trecho:**
  ```typescript
  // signUpAction — payload recebido diretamente do cliente
  export async function signUpAction(payload: {
    email: string; password: string;
    optionsData: {
      nome_completo: string;
      whatsapp: string;
      user_referredbycode: string | null;
      tipo: "comum" | "parceiro";
      commission_rate?: number;
    };
  }) {
    const { data, error } = await supabase.auth.signUp({
      email: payload.email,
      password: payload.password,
      options: { data: payload.optionsData },  // sem validação server-side do "tipo"
    });
  ```
  ```sql
  -- handle_new_user(): confia no raw_user_meta_data vindo do cliente
  IF (new.raw_user_meta_data->>'tipo') = 'parceiro' THEN
    v_tipo := 'parceiro'::public.tipo_usuario;
    v_comm_rate := 25.0;
    v_comm_model := 'recurring'::public.indication_type;
  ELSE
    v_tipo := 'comum'::public.tipo_usuario;
    v_comm_rate := 10.0;
    ...
  ```
  ```typescript
  // getClientLayoutDataAction(): parceiro NUNCA é bloqueado por trial
  if (finalTipo === 'parceiro') {
    return { ..., userType: 'parceiro', trialExpired: false, ... };
  }
  ```
- **Impacto:** O `tipo` do usuário é definido a partir de `user_metadata`, que é controlado pelo cliente no momento do `signUp`. Qualquer pessoa pode registrar uma conta informando `tipo: "parceiro"` (e `commission_rate` arbitrário). O trigger `handle_new_user` grava `tipo='parceiro'`, `commission_rate=25` e `commission_model='recurring'`; o `getClientLayoutDataAction` então libera o acesso sem trial e sem assinatura, redirecionando para o dashboard de parceiro com comissões recorrentes. Isso representa burla completa da monetização e atribuição indevida de privilégios.
- **Recomendacao:** Nunca confiar em `tipo`/`commission_rate` vindos do cliente. Remover esses campos do `optionsData` aceito no signup, forçar `tipo='comum'` no servidor (ou validar contra um allowlist/serviço administrativo) e criar a elevação a "parceiro" somente por fluxo administrativo autenticado (ex.: ação protegida por `is_admin` ou edge function com service role).

---

## Findings Altos

---

### ALTO-001: Políticas RLS de `usuarios` duplicadas em duas migrations
- **Severidade:** BAIXO (rebaixado pelo Validador Cético)
- **Validacao:** REBAIXADO de ALTO para BAIXO — duplicação sem divergência atual; risco de manutenção de RLS, não exposição ativa.
- **Arquivo(s):** supabase/migrations/20260701000000_core_users_auth_and_security.sql:28-46 e supabase/migrations/20260707000000_security_fixes_rls_and_storage.sql:25-38
- **Trecho:**
  ```sql
  -- 20260701000000
  CREATE POLICY "Admins can read all users"
  ON public.usuarios FOR SELECT
  TO authenticated
  USING (
    auth.uid() = id OR public.is_admin(auth.uid())
  );

  CREATE POLICY "Admins can update all users"
  ON public.usuarios FOR UPDATE
  TO authenticated
  USING (
    auth.uid() = id OR public.is_admin(auth.uid())
  );

  -- 20260707000000 (idêntico)
  CREATE POLICY "Admins can read all users"
  ON public.usuarios FOR SELECT
  TO authenticated
  USING (
    auth.uid() = id OR public.is_admin(auth.uid())
  );

  CREATE POLICY "Admins can update all users"
  ON public.usuarios FOR UPDATE
  TO authenticated
  USING (
    auth.uid() = id OR public.is_admin(auth.uid())
  );
  ```
- **Impacto:** Lógica de isolamento de tenants/segurança replicada em dois pontos. Correções futuras de RLS podem ser aplicadas em apenas uma migration e esquecidas na outra, deixando a tabela `usuarios` exposta ou com política inconsistente entre ambientes. Como a migration de segurança posterior recria as mesmas políticas, qualquer ajuste de regra de acesso fica frágil.
- **Recomendacao:** Centralizar a criação das políticas em uma única migration base e, nas migrations posteriores, apenas fazer `DROP`/`CREATE` quando a regra realmente mudar. Alternativamente, criar uma função SQL única (`apply_usuarios_rls_policies`) e reutilizá-la.

---

### ALTO-002: Políticas RLS de `simulacoes` duplicadas em duas migrations
- **Severidade:** BAIXO (rebaixado pelo Validador Cético)
- **Validacao:** REBAIXADO de ALTO para BAIXO — duplicação sem divergência atual; risco de manutenção de RLS, não exposição ativa.
- **Arquivo(s):** supabase/migrations/20260703000000_simulacoes_and_tracking.sql:35-55 e supabase/migrations/20260707000000_security_fixes_rls_and_storage.sql:53-73
- **Trecho:**
  ```sql
  -- 20260703000000
  CREATE POLICY "Admins can read all simulacoes"
  ON public.simulacoes FOR SELECT
  TO authenticated
  USING (
    usuario_id = auth.uid() OR public.is_admin(auth.uid())
  );

  CREATE POLICY "Users can delete own simulacoes"
  ON public.simulacoes FOR DELETE
  TO authenticated
  USING (
    usuario_id = auth.uid() OR public.is_admin(auth.uid())
  );

  -- 20260707000000 (recria as mesmas duas políticas)
  CREATE POLICY "Admins can read all simulacoes"
  ON public.simulacoes FOR SELECT
  TO authenticated
  USING (
    usuario_id = auth.uid() OR public.is_admin(auth.uid())
  );

  CREATE POLICY "Users can delete own simulacoes"
  ON public.simulacoes FOR DELETE
  TO authenticated
  USING (
    usuario_id = auth.uid() OR public.is_admin(auth.uid())
  );
  ```
- **Impacto:** Regras de acesso a imagens/simulações de pacientes (dados sensíveis) duplicadas. Divergência entre as duas versões pode causar permissão excessiva (leitura/deleção de simulações de outros usuários) ou bloqueio indevido, com risco direto à privacidade dos pacientes.
- **Recomendacao:** Manter a definição das políticas em um único arquivo de migração e remover a recriação redundante na migration de segurança; se for necessário alterar, alterar a política existente (ex.: `CREATE OR REPLACE POLICY` não existe no Postgres, então usar `DROP` + `CREATE` apenas quando a condição muda).

---

### ALTO-003: View `verificar_status_usuario` inteiramente duplicada em duas migrations
- **Severidade:** BAIXO (rebaixado pelo Validador Cético)
- **Validacao:** REBAIXADO de ALTO para BAIXO — duplicação sem divergência atual; risco de manutenção de regra de paywall, não exposição ativa.
- **Arquivo(s):** supabase/migrations/20260705000000_subscriptions_and_status_view.sql:58-116 e supabase/migrations/20260707000000_security_fixes_rls_and_storage.sql:76-135
- **Trecho:**
  ```sql
  -- 20260705000000
  CREATE OR REPLACE VIEW public.verificar_status_usuario AS
   SELECT u.id AS user_id,
      COALESCE((uc.company_id)::text, (u.id)::text, 'novo_usuario'::text) AS company_id,
      ...
      WHERE (u.id = auth.uid());

  -- 20260707000000 (mesma view, ~50 linhas idênticas, apenas para refletir is_admin)
  CREATE OR REPLACE VIEW public.verificar_status_usuario AS
   SELECT u.id AS user_id,
      COALESCE((uc.company_id)::text, (u.id)::text, 'novo_usuario'::text) AS company_id,
      ...
      WHERE (u.id = auth.uid());
  ```
- **Impacto:** A view decide `status_code`/`dias_restantes` (liberação de acesso ao app e paywall). Ter a mesma regra de negócio/semântica de acesso em dois lugares aumenta o risco de a versão "antiga" ser usada como referência em manutenção, causando divergência entre o que libera o trial e o que a aplicação espera. Correção em uma cópia pode não ser refletida na outra.
- **Recomendacao:** Manter uma única definição da view (de preferência na migration mais recente) e eliminar a cópia anterior. Para alterações futuras, executar `CREATE OR REPLACE VIEW` apenas no local canônico.

---

### ALTO-004: Resolução de papel de administrador replicada em múltiplas actions
- **Severidade:** MEDIO (rebaixado pelo Validador Cético de Qualidade)
- **Validacao:** REBAIXADO de ALTO para MEDIO — duplicação confirmada com regras divergentes entre os 4 pontos (checkAdminAccessAction, getClientLayoutDataAction, getUserProfileAction e create-portal), mas sem exposição ativa; trata-se de risco de manutenção/consistência de autorização.
- **Arquivo(s):** src/lib/admin/actions.ts:97-132, src/lib/auth/actions.ts:118-145, src/lib/perfil/actions.ts:52-75, supabase/functions/create-portal/index.ts:69-92
- **Trecho:**
  ```typescript
  // admin/actions.ts - checkAdminAccessAction
  const { data: usuarioData, error: uError } = await supabase
    .from("usuarios").select("tipo").eq("id", user.id).maybeSingle();
  if (!uError && usuarioData) {
    const tipo = (usuarioData.tipo || "").toLowerCase();
    if (tipo === "admin" || tipo === "super_admin") {
      return { isAdmin: true, userId: user.id, error: null };
    }
  }
  const { data: ucData } = await supabase
    .from("user_company").select("role").eq("user_id", user.id).eq("active", true).maybeSingle();
  if (ucData && ucData.role === "super_admin") {
    return { isAdmin: true, userId: user.id, error: null };
  }

  // auth/actions.ts - getClientLayoutDataAction
  const uTipo = (usuarioData?.tipo || "").toLowerCase();
  const ucRole = (ucData?.role || "").toLowerCase();
  const isSuperAdmin = uTipo === 'super_admin' || ucRole === 'super_admin';
  const isAdmin = isSuperAdmin || uTipo === 'admin';

  // perfil/actions.ts - getUserProfileAction
  const userRole = (uTipo === 'admin' || uTipo === 'super_admin')
    ? uTipo
    : (ucRole === 'admin' || ucRole === 'super_admin' ? ucRole : (ucRole || uTipo || null));

  // supabase/functions/create-portal/index.ts
  const userTipo = (userData?.tipo || "").toLowerCase();
  if (userTipo === "admin" || userTipo === "super_admin") {
    hasCompanyAccess = true;
  }
  ```
- **Impacto:** A definição de quem é admin está espalhada por 4 implementações com regras ligeiramente diferentes (umas verificam `user_company.role = 'super_admin'`, outras aceitam `admin` em `user_company`, outras só `usuarios.tipo`). Se uma regra de segurança mudar, é fácil corrigir apenas um arquivo e deixar outro validando permissão incorretamente — abrindo brecha de acesso indevido ao painel admin ou ao portal de cobrança.
- **Recomendacao:** Criar um helper único (ex.: `getCurrentUserRole()` / `requireAdminAccess()` em `src/lib/auth/roles.ts`) que consulte `usuarios.tipo` + `user_company.role` de forma centralizada e reutilizá-lo em todas as actions e na Edge Function.

---

## Findings Médios

---

### MEDIO-001: IDOR em `getProfileCompanyAction` permite obter `company_id` de qualquer usuário
- **Severidade:** MEDIO
- **Arquivo(s):** src/lib/perfil/actions.ts:~18
- **Trecho:**
  ```typescript
  export async function getProfileCompanyAction(userId: string) {
    const admin = getSupabaseAdmin();
    if (!admin) return userId;

    const { data: ucData } = await admin
      .from("user_company")
      .select("company_id")
      .eq("user_id", userId)
      .maybeSingle();

    if (ucData?.company_id) return ucData.company_id;

    const { data: companyData } = await admin
      .from("company")
      .select("id")
      .eq("id", userId)
      .maybeSingle();
    return companyData?.id ?? userId;
  }
  ```
- **Impacto:** A função é uma Server Action (`"use server"`) que recebe `userId` arbitrário do cliente e **não verifica se o `userId` corresponde ao usuário autenticado**. Ela usa o client com `SUPABASE_SERVICE_ROLE_KEY` (bypass de RLS) para consultar `user_company` e `company`. Qualquer usuário autenticado pode invocar a action diretamente e enumerar/descobrir o `company_id` vinculado a qualquer `user_id`, quebrando o isolamento entre tenants e viabilizando ataques de IDOR encadeados.
- **Recomendacao:** Validar a identidade dentro da action usando `const { data: { user } } = await supabase.auth.getUser()` e rejeitar se `userId !== user.id`. Para obter o `company_id` do próprio usuário, remover o parâmetro livre e derivá-lo da sessão autenticada.

---

### MEDIO-002: CORS permissivo (`Access-Control-Allow-Origin: *`) nas edge functions de faturamento
- **Severidade:** MEDIO
- **Arquivo(s):** supabase/functions/create-portal/index.ts:4-7; supabase/functions/create-checkout/index.ts:4-7
- **Trecho:**
  ```ts
  const corsHeaders = {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
  };
  ```
- **Impacto:** Qualquer origem pode invocar as edge functions. Combinado com a autenticação opcional do `create-checkout` (CRITICO-002), isso amplia o abuso de criação de sessões de pagamento e leitura/escrita de dados de faturamento a partir de páginas maliciosas.
- **Recomendacao:** Restringir `Access-Control-Allow-Origin` ao(s) domínio(s) da aplicação e manter `Access-Control-Allow-Credentials` desativado, já que a autenticação é feita por header.

---

### MEDIO-003: `createCheckoutSessionAction` não autentica o usuário e envia dados arbitrários para a edge function
- **Severidade:** MEDIO
- **Arquivo(s):** `src/lib/planos/actions.ts:76`
- **Trecho:**
  ```typescript
  export async function createCheckoutSessionAction(payload: {
    price_id: string; email: string; company_id: string; ...
  }) {
    const res = await fetch(`${supabaseUrl}/functions/v1/create-checkout`, {
      method: "POST",
      headers: { Authorization: `Bearer ${anonKey}`, apikey: anonKey },
      body: JSON.stringify(payload),
    });
  ```
- **Impacto:** A Server Action não chama `supabase.auth.getUser()` nem valida se `price_id`, `company_id` e `email` pertencem ao usuário autenticado. Qualquer pessoa pode invocá-la e forjar um payload. Se a edge function `create-checkout` confiar nesses campos, é possível criar sessões de checkout para terceiros, manipular `referral_code`/`company_id` ou gerar cobranças indevidas. O mesmo padrão ocorre no `handleOpenPortal` da página de perfil (`create-portal`).
- **Recomendacao:** Autenticar server-side antes de chamar a edge function, enviar o token de sessão do usuário (ou usar service role server-side) e revalidar `price_id`/`company_id` na edge function, ignorando valores fornecidos pelo cliente quando sensíveis.

---

### MEDIO-004: `uploadUserLogoAction` não valida tamanho nem conteúdo do base64 enviado
- **Severidade:** MEDIO
- **Arquivo(s):** `src/lib/perfil/actions.ts` (uploadUserLogoAction)
- **Trecho:**
  ```typescript
  export async function uploadUserLogoAction(base64Data: string, mimeType: string, fileExt: string) {
    ...
    const buffer = Buffer.from(base64Data, "base64");   // sem limite de tamanho
    const { error: uploadError } = await supabase.storage
      .from("logoEmpresa")
      .upload(path, buffer, { upsert: true, contentType: finalMimeType, ... });
  ```
- **Impacto:** Diferentemente da rota `api/perfil/upload-logo` (que limita a 3 MB e valida MIME), esta Server Action aceita uma string base64 arbitrária, sem checagem de tamanho nem de que o conteúdo é realmente uma imagem. Um usuário autenticado pode enviar payloads gigantes, causando consumo de memória/CPU no servidor (DoS), custo de storage e persistência de conteúdo arbitrário no bucket `logoEmpresa`.
- **Recomendacao:** Aplicar limite de tamanho (ex.: 3 MB) antes de decodificar, validar magic bytes/assinatura da imagem e o MIME real, rejeitar base64 inválido e normalizar a extensão com base no tipo detectado.

---

### MEDIO-005: `saveSimulationAction` sem validação de tamanho/tipo e com uploads não atômicos (arquivos órfãos)
- **Severidade:** MEDIO
- **Arquivo(s):** `src/lib/simulacoes/actions.ts:6-24` e `:75-150`
- **Trecho:**
  ```typescript
  function base64ToBuffer(base64: string): Buffer {
    const clean = base64.replace(/^data:image\/[a-z]+;base64,/, "");
    return Buffer.from(clean, "base64");   // sem limite de tamanho
  }
  ...
  const [uploadBefore, uploadAfter] = await Promise.all([
    supabaseServer.storage.from(bucket).upload(originalPath, bufferOriginal, { contentType: "image/png" }),
    supabaseServer.storage.from(bucket).upload(simuladaPath, bufferSimulado, { contentType: "image/png" }),
  ]);
  // Se uploadBefore falhar, uploadAfter pode ter sucesso -> órfão
  // Se o insert no banco falhar, os dois arquivos ficam órfãos
  ```
- **Impacto:** As duas imagens (original e simulada) são recebidas como base64 sem validação de tamanho ou de assinatura de imagem. Um usuário autenticado pode enviar payloads massivos (DoS/custo). Além disso, como os uploads são paralelos e o `insert` ocorre depois, falhas parciais deixam objetos órfãos no bucket `simulacoes` sem vínculo no banco, gerando acúmulo de storage e potencial vazamento de lixo não rastreado.
- **Recomendacao:** Validar tamanho máximo e assinatura das imagens; fazer upload sequencial com compensação (remover o que foi enviado em caso de falha) ou usar uma transação/limpeza no `catch`; mover o `insert` para antes/compensar falhas de storage.

---








### MEDIO-013: Padrão de guarda de autenticação copiado em diversas server actions
- **Severidade:** BAIXO (rebaixado pelo Validador Cético de Qualidade)
- **Validacao:** REBAIXADO de MEDIO para BAIXO — duplicação confirmada em 10+ pontos, porém com variações apenas cosméticas (exceção vs. retorno de erro/mensagem); risco de manutenção, sem falha ativa de autenticação.
- **Arquivo(s):** src/lib/auth/actions.ts:6-10, 43-45, 143-145, src/lib/perfil/actions.ts:35-40, 146-150, 177-182, src/lib/planos/actions.ts:41-44, src/lib/indique-e-ganhe/actions.ts:8-12, src/lib/simulacoes/queries.ts:15-20, src/lib/actions/simulacoes.ts:78-86 e 300-305, src/lib/admin/actions.ts:100-106 e 527-533
- **Trecho:**
  ```typescript
  const supabase = await createClient();
  const { data: { user }, error: authError } = await supabase.auth.getUser();
  if (authError || !user) {
    return { error: "Não autenticado", data: null };
  }
  ```
- **Impacto:** O guarda de autenticação está replicado em mais de 10 ações de servidor com pequenas variações (algumas lançam exceção, outras retornam `error`, mensagens diferentes). É o tipo de lógica de segurança que, se precisar mudar (ex.: tratamento de sessão expirada, log de tentativa, bloqueio de usuário), precisará ser alterada em todos os pontos; a inconsistência pode fazer com que uma action continue aceitando chamadas sem o mesmo rigor.
- **Recomendacao:** Criar um helper centralizado `requireUser()` / `getSessionUser()` em `src/lib/auth/` que retorne o usuário autenticado ou um erro padronizado, e substituir os blocos repetidos em todas as actions.

---

### MEDIO-014: Open Redirect via `return_url` não validado na Edge Function `create-portal`
- **Severidade:** MEDIO
- **Arquivo(s):** supabase/functions/create-portal/index.ts:~48
- **Trecho:**
  ```typescript
  const { company_id, return_url } = await req.json();
  // ...
  const session = await stripe.billingPortal.sessions.create({
    customer: customerData.stripe_customer_id,
    return_url: return_url || "https://app.dentixia.com/perfil",
  });
  ```
- **Impacto:** O `return_url` é aceito do corpo da requisição sem qualquer validação contra uma allowlist de domínios. Após o usuário sair do portal de faturamento do Stripe, ele pode ser redirecionado para um domínio controlado por atacante, criando um vetor de phishing/open redirect no fluxo de pagamento.
- **Recomendacao:** Validar `return_url` no servidor com uma allowlist estrita (ex.: apenas `https://app.dentixia.com` e subdomínios esperados) ou forçar sempre uma URL fixa/derivada do app, ignorando valores arbitrários do cliente.

---

## Findings Baixos

---

### BAIXO-001: Enumeração de usuários por mensagens de erro distintas
- **Severidade:** BAIXO
- **Arquivo(s):** src/app/login/page.tsx:18-43; src/lib/auth/actions.ts:66-69
- **Trecho:**
  ```ts
  if (msg.includes("email not confirmed")) {
    return "E-mail não confirmado. Verifique sua caixa de entrada para confirmar seu cadastro.";
  }
  ...
  if (msg.includes("user not found")) {
    return "Usuário não encontrado.";
  }
  ```
  ```ts
  if (!data.user) {
    return { error: "Este e-mail já está cadastrado. Faça login ou use a opção 'Esqueci minha senha'.", data: null };
  }
  ```
- **Impacto:** Mensagens diferentes para "e-mail não confirmado", "usuário não encontrado" e "e-mail já cadastrado" permitem a um atacante confirmar quais e-mails possuem conta, facilitando phishing direcionado e ataques de força bruta.
- **Recomendacao:** Usar mensagens genéricas e equivalentes para qualquer falha (ex.: "Credenciais inválidas" no login e "Se o e-mail existir, enviaremos instruções" no cadastro/reset), sem diferenciar o motivo.

---

### BAIXO-002: Política de senha fraca (mínimo de 6 caracteres)
- **Severidade:** BAIXO
- **Arquivo(s):** src/app/login/page.tsx:44-48; src/app/redefinir-senha/page.tsx:26
- **Trecho:**
  ```ts
  if (password.length < 6) {
    notify("Senha inválida", "A senha deve ter no mínimo 6 caracteres.", "warning");
    return;
  }
  ```
  ```ts
  const isStrong = newPassword.length >= 6;
  ```
- **Impacto:** Apenas 6 caracteres são exigidos, sem complexidade, o que facilita ataques de força bruta/credential stuffing contra contas com senhas fracas.
- **Recomendacao:** Elevar o mínimo para 8–12 caracteres e exigir combinação de maiúsculas, minúsculas, números e símbolos; considerar validação contra listas de senhas comuns e verificação de senha comprometida.

---

### BAIXO-003: Função `handle_new_user` `SECURITY DEFINER` sem `SET search_path`
- **Severidade:** BAIXO
- **Arquivo(s):** supabase/migrations/20260706000000_cleanup_and_referral_fix.sql:31
- **Trecho:**
  ```sql
  CREATE OR REPLACE FUNCTION public.handle_new_user()
  RETURNS trigger AS $$
  ...
  $$ LANGUAGE plpgsql SECURITY DEFINER;
  ```
- **Impacto:** A função é executada com privilégios do proprietário (definer) sem fixar `search_path`. Embora o código atual qualifique a maioria dos objetos com `public.`, a ausência de `SET search_path` é uma prática insegura que pode permitir sequestro de `search_path` se objetos não qualificados forem introduzidos no futuro.
- **Recomendacao:** Adicionar `SET search_path = public, pg_temp` (ou um schema fixo) à cláusula final da função e garantir que todos os objetos referenciados sejam totalmente qualificados.

---

### BAIXO-004: Leitura anônima de `system_settings`
- **Severidade:** BAIXO
- **Arquivo(s):** supabase/migrations/20260704000000_system_settings_and_notifications.sql:13-16
- **Trecho:**
  ```sql
  CREATE POLICY "Leitura de configuracoes do sistema"
  ON public.system_settings FOR SELECT
  TO public, authenticated, anon
  USING (true);
  ```
- **Impacto:** A tabela de configurações globais pode ser lida por qualquer pessoa, inclusive sem autenticação. Hoje armazena apenas `welcome_video_url`, mas a política permite que futuras chaves/configurações sensíveis fiquem expostas anonimamente.
- **Recomendacao:** Restringir o SELECT a `authenticated` (ou criar uma política específica apenas para chaves comprovadamente públicas) e manter o gerenciamento restrito a admins.

---

### BAIXO-005: Ausência de middleware de proteção de rotas e gate de admin apenas no cliente
- **Severidade:** BAIXO
- **Arquivo(s):** src/app/admin/page.tsx:52-67 (gate client-side); sem `middleware.ts` no projeto
- **Trecho:**
  ```ts
  useEffect(() => {
    async function verifyAccess() {
      const res = await checkAdminAccessAction();
      if (!res.isAdmin) {
        toast.error("Acesso Não Autorizado: Permissão restrita a administradores.");
        router.push("/login?error=unauthorized");
        return;
      }
      setAuthorized(true);
    }
    verifyAccess();
  }, [router]);
  ```
- **Impacto:** Não há `middleware.ts` para bloquear o acesso a rotas autenticadas/administrativas no servidor antes do render. Embora os dados estejam protegidos pelas server actions, a verificação de admin em `/admin` é feita somente no cliente, o que expõe a estrutura da página e depende de JavaScript para redirecionar.
- **Recomendacao:** Implementar um `middleware.ts` com validação de sessão (Supabase SSR) para proteger rotas sensíveis (`/admin`, `/perfil`, etc.) no servidor, mantendo as verificações nas server actions como segunda camada.

---

### BAIXO-006: Máscara de telefone/WhatsApp duplicada em quatro componentes
- **Severidade:** BAIXO
- **Arquivo(s):** src/components/ClientLayout.tsx:206-218, src/app/register/page.tsx:52-66, src/app/register/parceiros/page.tsx:36-50, src/app/perfil/page.tsx:530-538
- **Trecho:**
  ```typescript
  const digits = e.target.value.replace(/\D/g, "").slice(0, 11);
  let masked = digits;
  if (digits.length > 2) masked = `(${digits.slice(0, 2)}) ${digits.slice(2)}`;
  if (digits.length > 7) {
    const body = digits.length === 11
      ? `${digits.slice(2, 7)}-${digits.slice(7)}`
      : `${digits.slice(2, 6)}-${digits.slice(6)}`;
    masked = `(${digits.slice(0, 2)}) ${body}`;
  }
  ```
- **Impacto:** Código utilitário repetido, com variações (a versão de `perfil` adiciona um passo extra para 11 dígitos que produz máscara inconsistente). Dificulta padronizar o formato aceito para WhatsApp em toda a aplicação.
- **Recomendacao:** Extrair `maskPhone(value: string): string` para `src/lib/utils.ts` (ou módulo de formatação) e reutilizar nos quatro pontos.

---

### BAIXO-007: Condições idênticas nas políticas de storage `logoEmpresa` (INSERT/UPDATE/DELETE)
- **Severidade:** BAIXO
- **Arquivo(s):** supabase/migrations/20260702000000_storage_buckets_and_rls.sql:61-105
- **Trecho:**
  ```sql
  -- Logo INSERT proprio usuario
  WITH CHECK (
    bucket_id = 'logoEmpresa'
    AND auth.role() = 'authenticated'
    AND (
      split_part(name, '.', 1) = auth.uid()::text
      OR
      (storage.foldername(name))[1] = auth.uid()::text
    )
  );

  -- Logo UPDATE proprio usuario (mesma condição)
  USING (
    bucket_id = 'logoEmpresa'
    AND auth.role() = 'authenticated'
    AND (
      split_part(name, '.', 1) = auth.uid()::text
      OR
      (storage.foldername(name))[1] = auth.uid()::text
    )
  );

  -- Logo DELETE proprio usuario (mesma condição)
  USING (
    bucket_id = 'logoEmpresa'
    AND auth.role() = 'authenticated'
    AND (
      split_part(name, '.', 1) = auth.uid()::text
      OR
      (storage.foldername(name))[1] = auth.uid()::text
    )
  );
  ```
- **Impacto:** A mesma regra de propriedade de arquivo é repetida três vezes no mesmo arquivo. Uma correção de segurança (ex.: permitir admins removerem logos) precisaria ser replicada manualmente nas três políticas, com risco de esquecimento.
- **Recomendacao:** Criar uma função SQL auxiliar (ex.: `public.can_access_logo_object(name text)`) que encapsule a condição e referenciá-la nas três políticas.

---

### BAIXO-008: `handle_new_user()` redefinida com lógica de consentimento e lookup de e-mail duplicada
- **Severidade:** BAIXO
- **Arquivo(s):** supabase/migrations/20260701000000_core_users_auth_and_security.sql:70-133 e supabase/migrations/20260706000000_cleanup_and_referral_fix.sql:30-112
- **Trecho:**
  ```sql
  -- 20260701000000
  SELECT id INTO existing_user_id FROM public.usuarios WHERE email = new.email LIMIT 1;
  IF existing_user_id IS NULL THEN
    INSERT INTO public.usuarios (...) VALUES (...);
    existing_user_id := new.id;
  END IF;
  SELECT EXISTS(SELECT 1 FROM public.consentimentos WHERE user_id = existing_user_id) INTO has_consent;
  IF NOT has_consent THEN
    INSERT INTO public.consentimentos (user_id, aceitou_em, versao_politica) VALUES (existing_user_id, now(), '1.0');
  END IF;

  -- 20260706000000 (mesma lógica de busca por e-mail + garantia de consentimento)
  SELECT id INTO existing_user_id FROM public.usuarios WHERE email = new.email LIMIT 1;
  IF existing_user_id IS NULL THEN
    INSERT INTO public.usuarios (...) VALUES (...);
    existing_user_id := new.id;
  END IF;
  SELECT EXISTS(SELECT 1 FROM public.consentimentos WHERE user_id = existing_user_id) INTO has_consent;
  IF NOT has_consent THEN
    INSERT INTO public.consentimentos (user_id, aceitou_em, versao_politica) VALUES (existing_user_id, now(), '1.0');
  END IF;
  ```
- **Impacto:** A função de trigger `handle_new_user` é redefinida por completo com a mesma base de lógica. A existência de duas versões históricas grandes dificulta entender qual é a regra vigente de criação de usuário/consentimento e aumenta a chance de manutenção na versão errada.
- **Recomendacao:** Consolidar a definição final de `handle_new_user()` em uma única migration e remover a duplicação da versão anterior (ou reduzir a segunda a um `CREATE OR REPLACE` que altere apenas os trechos realmente novos, reutilizando funções auxiliares para consentimento).

---

### BAIXO-009: `getReferralDataAction` gera código de indicação com `Math.random` e sofre race condition
- **Severidade:** BAIXO
- **Arquivo(s):** `src/lib/indique-e-ganhe/actions.ts:4-7` e `:20-40`
- **Trecho:**
  ```typescript
  function generateDTReferralCode(): string {
    const digits = Math.floor(100000 + Math.random() * 900000);
    return `DT${digits}`;
  }
  ...
  if (!refCode || !dtCodeRegex.test(refCode)) {
    refCode = generateDTReferralCode();
    await supabase.from('usuarios').update({ referral_code: refCode }).eq('id', user.id);
  }
  ```
- **Impacto:** `Math.random()` não é criptográfico e não há restrição de unicidade no banco nem retry em caso de conflito. Requisições concorrentes do mesmo usuário podem gerar códigos diferentes e o último `update` vence (race), podendo ainda colidir entre usuários e quebrar a rastreabilidade de indicações.
- **Recomendacao:** Gerar o código com fonte criptográfica (`crypto.randomInt`) e/ou criar uma coluna com `UNIQUE` + retry em conflito; idealmente gerar o código no trigger/banco de forma atômica.

---

### BAIXO-010: Fire-and-forget com `.then(() => {})` sem `.catch` gera unhandled rejection
- **Severidade:** BAIXO
- **Arquivo(s):** `src/lib/perfil/actions.ts` (getUserProfileAction)
- **Trecho:**
  ```typescript
  if (!profile) {
    supabase.from("usuarios").upsert({ ... }, { onConflict: "id" }).then(() => {});
  }
  ```
- **Impacto:** A operação é intencionalmente fire-and-forget, mas não possui `.catch()`. Se a promise rejeitar (falha de rede/RLS), o Node emitirá `unhandledRejection`, podendo derrubar o processo em runtimes/configurações estritas e mascarar falhas de auto-sync do perfil.
- **Recomendacao:** Adicionar `.catch((err) => console.warn(...))` ou transformar em chamada `try/catch` aguardada.

---

### BAIXO-011: `check_video` nunca é persistido — modal de boas-vindas reaparece em todo login
- **Severidade:** BAIXO
- **Arquivo(s):** `src/components/ClientLayout.tsx` (handleCloseWelcomeVideo), `src/lib/auth/actions.ts` (setCheckVideoAction)
- **Trecho:**
  ```typescript
  const handleCloseWelcomeVideo = async () => {
    setShowWelcomeVideo(false);
    // setCheckVideoAction(true) NUNCA é chamado
  };
  ```
- **Impacto:** A action `setCheckVideoAction` existe e é importada, mas nunca é invocada ao fechar o vídeo de boas-vindas. O campo `check_video` permanece `false`, então o modal é exibido novamente a cada sessão/login, gerando comportamento repetitivo e inconsistência com a regra de exibição única.
- **Recomendacao:** Chamar `await setCheckVideoAction(true)` no `handleCloseWelcomeVideo` (com tratamento de erro) para persistir a flag e impedir reexibição.

---

### BAIXO-012: `deleteSimulationAction` remove o registro do banco antes do storage, podendo deixar órfãos
- **Severidade:** BAIXO
- **Arquivo(s):** `src/lib/simulacoes/actions.ts` (deleteSimulationAction)
- **Trecho:**
  ```typescript
  // Deletar do banco de dados com filtro estrito de usuário
  const { error: deleteDbError } = await supabaseServer
    .from("simulacoes").delete().eq("id", id).eq("usuario_id", user.id);
  ...
  // Deletar imagens do storage
  try {
    ...
    await supabaseServer.storage.from(bucket).remove(pathsToDelete);
  } catch (storageErr) {
    console.error("Erro ao remover arquivos do storage:", storageErr);
  }
  ```
- **Impacto:** Se a remoção no storage falhar após o `delete` no banco ter sucesso, os arquivos de imagem permanecem órfãos no bucket `simulacoes` sem registro correspondente. Como as fotos são dados sensíveis de pacientes, isso representa retenção indevida e acúmulo de storage.
- **Recomendacao:** Remover os arquivos do storage primeiro e só então apagar o registro (com compensação reversa), ou registrar falhas de limpeza para retry/expurgo assíncrono.

---

### BAIXO-013: `getClientLayoutDataAction` falha aberto (fail-open) quando a view de status retorna `null`
- **Severidade:** BAIXO
- **Arquivo(s):** `src/lib/auth/actions.ts` (getClientLayoutDataAction)
- **Trecho:**
  ```typescript
  const { data: statusData } = await supabase
    .from('verificar_status_usuario')
    .select('status_code')
    .maybeSingle();

  const trialExpired = statusData ? statusData.status_code !== 3 : false;
  ```
- **Impacto:** Se a view `verificar_status_usuario` não retornar linha (ex.: usuário autenticado sem registro correspondente em `public.usuarios`), `statusData` é `null` e `trialExpired` recebe `false`, liberando o acesso. É uma lógica fail-open: a ausência de dados de status é tratada como "acesso liberado" em vez de "bloquear/validar".
- **Recomendacao:** Tratar `statusData === null` como cenário de erro ou assumir `trialExpired = true` (fail-closed), forçando a sincronização/criação do perfil antes de liberar o acesso.

---

### BAIXO-014: RLS permite que qualquer usuário autenticado leia todas as notificações do sistema
- **Severidade:** BAIXO
- **Arquivo(s):** supabase/migrations/20260704000000_system_settings_and_notifications.sql:~39
- **Trecho:**
  ```sql
  CREATE POLICY "Authenticated users can read notifications"
  ON public.notifications_history FOR SELECT
  TO authenticated
  USING (true);
  ```
- **Impacto:** A tabela `notifications_history` fica legível integralmente para qualquer usuário autenticado que acesse o banco diretamente (via API Supabase), ignorando a segmentação por `target_audience` feita apenas na Server Action. Notificações destinadas a segmentos específicos (ex.: avisos internos, promoções segmentadas) podem ser lidas por qualquer conta autenticada.
- **Recomendacao:** Restringir a policy de SELECT para considerar o público-alvo da notificação com base no perfil do usuário autenticado, ou expor a leitura somente por meio de uma RPC/função com lógica de autorização e manter a tabela sem leitura direta para `authenticated`.

---

### BAIXO-015: `salvarSimulacaoConfirmada` persiste URLs fornecidas pelo cliente sem validação de origem
- **Severidade:** BAIXO
- **Arquivo(s):** src/lib/actions/simulacoes.ts:~245
- **Trecho:**
  ```typescript
  const { error: dbError } = await supabase.from("simulacoes").insert({
    usuario_id: user.id,
    procedimento: procedimento,
    img_original_url: urlOriginal,
    img_simulada_url: urlSimulada,
    nome_paciente: nomePaciente.trim(),
    cor_utilizada: corUtilizada,
  });
  ```
- **Impacto:** As URLs `urlOriginal` e `urlSimulada` vêm diretamente do cliente e são persistidas sem verificar se pertencem ao bucket `simulacoes` do próprio usuário. Isso permite gravar URLs externas que serão renderizadas como `<img>` na galeria e no comparador, possibilitando injeção de conteúdo visual de terceiros, rastreamento do usuário (ex.: pixels de imagem) e referências inválidas no histórico.
- **Recomendacao:** Validar que as URLs pertencem ao bucket/domínio esperado do storage e, idealmente, reconstruí-las no servidor a partir de chaves/paths retornados pela geração, em vez de confiar em URLs arbitrárias enviadas pelo cliente.

---

`RELATORIO COMPLETO`

---

## Validacao Cetica de Qualidade - Sumario Parcial

### Escopo
Validacao das secoes 04 (Duplication Detector), 05 (Logic Analyzer) e 06 (Standards Checker) contra o codigo real.

### Totais

| Metrica | Quantidade |
|---------|-----------|
| CONFIRMADO | 16 |
| REMOVIDO | 0 |
| REBAIXADO | 2 |

### Decisoes por finding

**Secao 04 - Duplication Detector**
- ALTO-001: CONFIRMADO (rebaixamento previo para BAIXO mantido)
- ALTO-002: CONFIRMADO (rebaixamento previo para BAIXO mantido)
- ALTO-003: CONFIRMADO (rebaixamento previo para BAIXO mantido)
- ALTO-004: REBAIXADO para MEDIO
- MEDIO-013: REBAIXADO para BAIXO
- BAIXO-006: CONFIRMADO
- BAIXO-007: CONFIRMADO
- BAIXO-008: CONFIRMADO

**Secao 05 - Logic Analyzer**
- MEDIO-004: CONFIRMADO
- MEDIO-005: CONFIRMADO
- BAIXO-009: CONFIRMADO
- BAIXO-010: CONFIRMADO
- BAIXO-011: CONFIRMADO
- BAIXO-012: CONFIRMADO
- BAIXO-013: CONFIRMADO
- BAIXO-015: CONFIRMADO

**Secao 06 - Standards Checker**
- BAIXO-002: CONFIRMADO
- BAIXO-005: CONFIRMADO

### Observacoes
- Nenhum falso positivo foi removido nas secoes de qualidade; todos os findings verificados existem no codigo real.
- A tabela-resumo de severidades no topo ainda reflete contagens pre-deduplicacao (ex.: MEDIO=14, enquanto o corpo lista 7 findings MEDIO). Isso nao afeta as decisoes acima e pode ser reconciliado na consolidacao final.
- BAIXO-005 e essencialmente um gap de cobertura (ausencia de middleware), mas foi validado como finding existente e mantido como BAIXO.
